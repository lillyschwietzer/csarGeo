% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/countryside_sar.R
\name{countryside_sar}
\alias{countryside_sar}
\title{Countryside-SAR}
\usage{
countryside_sar(
  data,
  crs,
  method = c("circles", "clusters"),
  radius = NULL,
  break_threshold = 0.5,
  custom_hull = NULL,
  square_size = NULL,
  cluster_sizes = NULL,
  habitat = NULL,
  habitat_names = NULL,
  classification = NULL,
  groups = NULL,
  seed = NULL,
  transform_to_utm = FALSE,
  target_crs = NULL,
  warn_projection = TRUE,
  n_runs = 1
)
}
\arguments{
\item{data}{Binary species data table, the first column is the location ID, second and third columns are longitude and latitude values of the sampling locations, columns 4 and onward contain binary presence/absence data of the species.}

\item{crs}{Coordinate reference system (CRS) of the sampling location.}

\item{method}{Character string specifying the sampling method. Options:
\itemize{
\item \code{"circles"}: Samples species data within expanding circles
from a random starting point. Sampling is limited by a convex hull defined by
\code{custom_hull}.
\item \code{"clusters"}: Assigns each sampling point a square of size
\code{square_size}.
}}

\item{radius}{Numeric vector defining the radii for method = "circles". E.g. c(2000 * 1:10), sample in 10 circles with an extent of 2000 units each.}

\item{break_threshold}{Defines break-protocol-sensitivity for method "circles" based on the proportion of the circular vector that lies inside the convex hull. E.g. break_threshold = 0.9 -> if less than 90 \% of the circle lies inside of the hull, stop sampling.}

\item{custom_hull}{Import a polygon hull for method "circles". If method = "clusters" the function will ignore the imported hull and auto-generate a hull instead. If custom_hull = NULL, the function auto-generates a hull for method "circles".}

\item{square_size}{The size of the initial square buffer for each sampling point for method "clusters".}

\item{cluster_sizes}{Numerical vector that defines the amount of levels as well as the amount of sampling points within each cluster of each level for the hierarchical "clusters" approach. E.g. c(1, 4, 16, 64, 256) -> 5 levels for 256 sampling points; first level 256 clusters, second level 64 clusters, etc.}

\item{habitat}{Land-use raster of the sampling location, a .tif file.}

\item{habitat_names}{Character vector with the names of the land-use types 'habitat' land-use raster, legend of habitat.}

\item{classification}{Species classification file. A table with a first column for species names and the following columns as binary (0/1) group indicators.}

\item{groups}{Character vector to define the group columns used for analysis. If NULL = use all columns except first.}

\item{seed}{Optional seed for reproducibility.}

\item{transform_to_utm}{If TRUE, transforms geographic coordinates (long/lat) to UTM projection. It automatically detects the appropriate UTM zone based on mean longitude. If data was sampled in polar, equatorial or very large regions, use an appropriate projection instead of UTM.}

\item{target_crs}{Optional numeric EPSG code for coordinate transformation. If provided it overrides the automatic UTM zone detection. Use this when you need a specific national or local projection instead of UTM.}

\item{warn_projection}{Defaults to TRUE, will warn the user about possible projection issues.}

\item{n_runs}{Option to run the analyis multiple times, default value = 1. Only available for method "circles" as "clusters" is deterministic, multiple runs produce the same result.}
}
\value{
A list containing the method used, the number of runs n_runs, the sampling data of each run, the sf-transformed input data with an added geometry column as well as information about the convex hull. The sampling data of each run contains a results_table of aggregated habitat area data and species richness data. It further contains the SAR analysis result and linear model summary as well as geometry data of each circle or cluster and species data within each circle or cluster level.
}
\description{
A function to perform a complete classic SAR analysis or a countryside SAR (cSAR) analysis. It contains two analysis pathways: a nested "circles" approach, or a hierarchical "clusters" approach. Using "circles", the function samples species data in step wise increasing circles, while "clusters" groups it in clusters of increasing size based on their proximity to one another. The sampled data is then aggregated and used for the SAR or cSAR analysis, the latter includes habitat affinity values of the species groups to different habitat types.
}
\examples{
\dontrun{
res <- countryside_sar(
  data = mydata,
  crs = 3763,
  method = "circles",
  radius = 2000 * 1:10,
  habitat = myraster,
  habitat_names = c("Forest", "Agriculture", "Shrubland"),
  habitat_codes = 1:3,
  classification = myclassif,
  groups = "Forest",
  seed = 123
)
}
}
