% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backup_countryside_sar.R
\name{countryside_sar2}
\alias{countryside_sar2}
\title{Countryside-SAR}
\usage{
countryside_sar2(
  data,
  crs,
  method = c("circles", "clusters"),
  radius = NULL,
  break_threshold = 0.5,
  custom_hull = NULL,
  square_size = NULL,
  cluster_sizes = NULL,
  habitat = NULL,
  habitat_names = NULL,
  habitat_codes = NULL,
  classification = NULL,
  groups = NULL,
  seed = NULL,
  transform_to_utm = FALSE,
  target_crs = NULL,
  warn_projection = TRUE,
  n_runs = 1
)
}
\arguments{
\item{data}{The provided dataset. It needs to be structured as following: the first column is the location ID, second and third columns are longitude and latitude values of the sampling locations, columns 4 and onward contain binary presence/absence data of the species.}

\item{crs}{The coordinate reference system (CRS) of the sampling location.}

\item{method}{Parameter to chose one analysis method: "circles" or "clusters". Both methods begin by transforming the table into an sf object and adding a joined geometry column. Pathway "Circles" then samples species data within a defined hull around all sampling points, beginning with a random starting point and in the steps defined by the parameter radius. The species count of all circles is then aggregated into one result table. Pathway "Clusters" assigns each sampling point a standardized square with the extent of square_size. The squares are then grouped based on cluster_sizes and the species data is again aggregated into a table.}

\item{radius}{Defines the radius size as well as the total amount of circles for method: "Circles"}

\item{break_threshold}{Initiates break-protocol for method "Circles" based on the proportion of the circular vector that lies inside the convex hull. E.g. break_threshold = 0.9 -> if less than 90 \% of the circle lies inside of the hull, stop.}

\item{custom_hull}{Import a polygon hull for method "circles". If method = "clusters" the function will ignore the imported hull and auto-generate a hull instead. If custom_hull = NULL, the function auto-generates a hull for method "circles".}

\item{square_size}{The size of the square buffer for each sampling point for method "Clusters".}

\item{cluster_sizes}{Numerical vector that defines the amount of levels for the hierarchical "Clusters" approach as well as the amount of clusters within each level, e.g. c(1, 4, 16, 64, 256) -> 5 levels, first level 256 clusters, second level 64 clusters, etc.}

\item{habitat}{Land-use raster of the sampling location, a .tif file.}

\item{habitat_names}{Character vector with the names of the land-use types of the land-use raster defined in 'habitat'.}

\item{classification}{Species classification file. A table with a first column for species names and the following columns as binary (0/1) group indicators.}

\item{groups}{Which group columns to use from classification (NULL = use all columns except first).}

\item{seed}{Optional seed for reproducibility of random processes (circles starting point and k-means clustering).}

\item{transform_to_utm}{If TRUE, transforms geographic coordinates (long/lat) to UTM projection. It automatically detects the appropriate UTM zone based on mean longitude. If data was sampled in polar, equatorial or very large regions, please use an appropriate projection instead of UTM.}

\item{target_crs}{Optional numeric EPSG code for coordinate transformation. If provided it overrides the automatic UTM zone detection. Use this when you need a specific national or local projection instead of UTM.}

\item{warn_projection}{Defaults to TRUE, will warn the user about possible projection issues.}

\item{n_runs}{Option to run the function multiple times, default value = 1.}
}
\value{
A list containing the method used, the number of runs n_runs, the sampling data of each run, the sf-transformed input data with an added geometry column as well as information about the convex hull. The sampling data of each run contains a results_table of aggregated habitat area data and species richness data. It further contains the SAR analysis result and linear model summary as well as geometry data of each circle or cluster and species data within each circle or cluster level.
}
\description{
A function to perform a complete SAR analysis using binary species presence/absence data. It contains two analysis pathways: a nested "circles" approach, or a hierarchical "clusters" approach, which assigns standardized squares to each sampling point and groups them in increasing size and decreasing count for each level.

See Details for more information.
}
\examples{
\dontrun{
res <- countryside_sar(
  data = mydata,
  crs = 3763,
  method = "circles",
  radius = 2000 * 1:10,
  habitat = myraster,
  habitat_names = c("Forest", "Agriculture", "Shrubland"),
  habitat_codes = 1:3,
  classification = myclassif,
  groups = "Forest",
  seed = 123
)
}
}
